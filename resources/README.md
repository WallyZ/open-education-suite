# Resources

This core repo should not own domain resource libraries.

Subject resources live in sibling content repos listed in `content-sources.json`. Keep this folder for platform-neutral resource ingestion notes or adapters only.
