{
  "schemaVersion": 1,
  "policyId": "lecture-media-archive-policy-v1",
  "localArchiveRoot": "generated-lectures",
  "packagePathPattern": "{lectureSlug}\\",
  "requiredManifestFields": [
    "media[].assetId",
    "media[].type",
    "media[].path",
    "media[].sha256",
    "media[].status",
    "media[].requiredForPublish"
  ],
  "checksumAlgorithm": "sha256",
  "checksumRule": "Rendered or archived media must record a 64-character SHA-256 checksum in lecture-video.json before publish.",
  "renderedMediaGitPolicy": "do-not-commit-rendered-media",
  "largeFileStorageDecision": "Store rendered lecture media under the owning subject content repo generated-lectures folder and ignore large binaries for now; defer Git LFS or object storage until a publishing workflow needs versioned media distribution.",
  "allowedStatuses": [
    "planned",
    "rendered",
    "archived"
  ],
  "requiredForPublishRule": "Required lecture media must be rendered or archived locally with checksums before the course can depend on it.",
  "forbiddenLocations": [
    "fixtures",
    "docs",
    "ui",
    "study-plans",
    "resources"
  ],
  "notes": [
    "Keep manifests, scripts, captions, transcripts, storyboard, and QA metadata in the owning subject content repo.",
    "Keep large binary audio and video files out of Git unless this policy is replaced by an explicit large-file storage decision.",
    "External hosted videos may be optional references, but required instruction must have local rendered or archived media."
  ]
}
